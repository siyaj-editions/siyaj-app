{% extends 'base.html.twig' %}

{% block title %}Modifier auteur - SIYAG{% endblock %}

{% block body %}
<h1 class="mb-4">Modifier l'auteur : {{ author.name }}</h1>

{{ form_start(authorForm) }}
    {{ form_widget(authorForm) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button>
    <a href="{{ path('app_admin_author_index') }}" class="btn btn-secondary">Annuler</a>
{{ form_end(authorForm) }}
{% endblock %}
