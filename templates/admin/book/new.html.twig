{% extends 'base.html.twig' %}

{% block title %}Nouveau livre - SIYAG{% endblock %}

{% block body %}
<section class="py-12 md:py-16">
    <div class="container max-w-4xl space-y-6">
        <div>
            <p class="text-sm text-sage">Administration</p>
            <h1 class="text-4xl md:text-5xl text-ink">Nouveau livre</h1>
        </div>

        <section class="card p-6 md:p-8">
            {{ form_start(bookForm, {'attr': {'class': 'space-y-6'}}) }}
                {{ form_widget(bookForm) }}
                <div class="flex flex-col sm:flex-row gap-3 pt-4 border-t border-black/5">
                    <button type="submit" class="btn btn-primary">Créer</button>
                    <a href="{{ path('app_admin_book_index') }}" class="btn btn-secondary">Annuler</a>
                </div>
            {{ form_end(bookForm) }}
        </section>
    </div>
</section>
{% endblock %}
